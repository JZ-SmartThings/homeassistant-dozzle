release: v0.1.2

- fix(ingress): nginx reverse proxy — Dozzle on :8081, nginx on :8080
- fix(ingress): sub_filter patches absolute /assets/ → ./assets/ in HTML
- fix(ingress): JS shim injected in <head> rewrites fetch/XHR/WebSocket paths
- fix(ingress): SSE streaming unbuffered (proxy_buffering off for /api/*)
